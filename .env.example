# Supabase (required - get these from your Supabase project dashboard)
VITE_SUPABASE_URL=YOUR_SUPABASE_URL
VITE_SUPABASE_ANON_KEY=YOUR_SUPABASE_ANON_KEY

# M-Pesa Daraja API (optional - sandbox defaults)
# In production, M-Pesa credentials live on the Edge Function (server-side only)
# Remove these from .env.local once Edge Functions are configured
VITE_MPESA_CONSUMER_KEY=
VITE_MPESA_CONSUMER_SECRET=
VITE_MPESA_PASSKEY=
VITE_MPESA_SHORTCODE=247247
VITE_MPESA_ENV=sandbox

# Stripe (optional - needed for card payments)
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_xxx

# Site URL (used for redirects and M-Pesa callbacks)
VITE_APP_URL=https://chama-os.vercel.app
